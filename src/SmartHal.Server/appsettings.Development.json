{
  "Serilog": {
    "MinimumLevel": {
      "Override": {
        // Every SmartHal component is verbose while developing; the rest keeps the default level.
        "SmartHal": "Debug"
      }
    },
    "WriteTo": {
      "Console": {
        "Args": {
          // Development is the one environment that reads the console itself, so the compact JSON of
          // appsettings.json gives way to readable text (FR-29, NFR-6).
          "formatter": {
            "type": "Serilog.Formatting.Display.MessageTemplateTextFormatter, Serilog",
            "outputTemplate": "[{Timestamp:HH:mm:ss} {Level:u3}] {SourceContext} {Message:lj}{NewLine}{Exception}"
          }
        }
      }
    }
  }
}
